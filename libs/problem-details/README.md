# problem-details

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build problem-details` to build the library.

## Running unit tests

Run `nx test problem-details` to execute the unit tests via [Jest](https://jestjs.io).
